1. 注册用户时候不成功，扫描二维码，基本信息，第一个用户每个页面不点保存，虽然到第三个页面保存失败了，
   但是userlist有了。但是其他两个tab都空了，基本信息tab有信息。
   如果没有tab都保存，就成功了。 OK
2. 数据库中有10个商品编码，设定了一个商品编码权限，他还是全部能看到。用户期间和商品期间确认一下。
3. 欧亚信息数据在二维码下面表示 改成先进 OK
4. 右上角登录 9090端口去掉 改成80 OK
5. 数据table 列要动态 数据统计按钮边上 做一个下拉菜单，多选出来header列，来显示到data table。 OK
6. 查看详细 一下子都选全部
7.导入数据后，需要重新登录，在编辑用户时，才可以看到新导入的商品编码。

8.删除用户，或者用户到期判断。如果到期后，再想续约的话，重新编辑该用户的基本信息，这个方式可好。
9. 查询数据，集计报表，下载Excel，汇总报表导出的用户权限需要确认。*（权限设定需要确认）
10.2003文件导入内存消耗的问题没有改。仿照2007来进行改修。
11. 导出报表选择的对话框还显示对话框，在下面显示了，改成对话框里面提示。OK
12. 导出报表按钮按下，getListValueWithPage按钮按下，选择报表头按钮，这几个没有对应timeout
13. 数据导入的时候，每10000条数据根据数据字典更新，但是如果数据字典中的字段在原始表eurasiatable中没有的话，更新就会报错，
     虽然如果用户不随便更改原始表eurasiatable的表结构，就不会报错，但是还是最好在根据数据字典更新之前判断原始表eurasiatable
     中有没有数据字典需要的表，如果没有，那么忽略数据字典更新。
14. 目前excel report的时候，默认的report type是全部在界面上选中的，应该按照用户给的模板设置默认选中。
15. batch update 的时候 exception出力错误时候，没有slfLogUtil，而是printStack了，要改善.
16. add column的时候，第一次很容易报错。
17. default用户的密码在创建用户的时候不要传到前端，只有密码在页面上必须填写项。
18. 连续用户删除的话，删除不掉。OK
19. UserID显示全部，不用tips。
20. 编辑用户的用户时候，海关编码增加一个全选功能。OK
21. 用户在选择期间的时候，可以选择无限期间功能。增加一个RadioButton? OK
22.调查bug，偶尔增加用户，扫描二维码之后，logout问题。 ※  -> 很难再现，考虑是广告拦截选项开启了。 OK
23. 数据表去重复的时候，用小数据左连接主表去重之后，在插入主表，改善性能。※ OK
24. 过期用户微信login的提示文言为未注册，要改成已经过期。